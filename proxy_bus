<?php
$cmd = "/usr/bin/w3m -dump 'http://wap.ratp.fr/siv/schedule?service=next&reseau=bus&lineid=B46&stationid=46_12_64' | grep -A 1 \">\" | sed '/--/d' | sed 's/> //' | sed 's/  //g'";
exec($cmd,$res);
//print_r($res);
$res = $res[5];
$a = explode(' ',$res);
$result = intval($a[0]);
if ($_GET['debug']) echo $result . " min direction gare du nord<br/>";
if ($result <= 3) {
        echo "1";
}else {
        echo "0";
}
?>
